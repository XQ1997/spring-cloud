spring:
  application:
    name: system-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/xc_system?serverTimezone=UTC&userUnicode=true&useSSL=false&
    username: root
    password: rootroot
# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml


